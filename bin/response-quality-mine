#!/usr/bin/env bash
# Usage: bin/response-quality-mine [--repo <repo-id>] [--top-n N]
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
PY="${ROOT}/.venv/bin/python"
if [ ! -x "$PY" ]; then
    PY="$(command -v python3)"
fi
"$PY" "$ROOT/scripts/learning/cli_response_quality_mine.py" "$@"
