# Runtime state (per-repo archives, packets, contexts, actions, memory, CS RAG index)
state/

# Remote build artifacts (downloaded LanceDB index roots, large binaries)
artifacts/

# HuggingFace model cache (sentence-transformers, cross-encoder)
.hf_cache/
.cache/huggingface/

# Learner mission repos (cloned locally, never tracked)
missions/

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp
*.swo

# Logs / tmp
*.log
tmp/
.tmp/

# Regenerated Korean rewrite fixture derivatives
tests/fixtures/cs_rag_golden_queries_ko_*_applied.json

# Claude Code runtime locks
.claude/scheduled_tasks.lock

# Secrets / local overrides
.env
.env.*
*.local
